# Backend Reference Implementation (Python FastAPI)

This directory contains a standalone reference implementation of the watermarks-remover engine in Python (`FastAPI`).

> **Note**: In the production SaaS environment (Vercel), the application runs natively on Next.js TypeScript engine (`lib/engine/*`). This directory is maintained for standalone Docker microservice deployments and Python CLI parity testing.
