# AGENTS.md

「未記入」の欄が残っている間は、`setup` を実行する。該当が無い欄は「なし」と書く。

## 公開前提（ポートフォリオ）

このリポジトリは、他人（採用担当者・他の開発者等）が中身をいつでも見る前提で開発する。
**このリポジトリをコピーして別の開発を1から始めた場合も、この節だけはそのまま引き継いで守る**
（プロジェクト固有の設定ではなく、開発姿勢そのものの基本ルールとして扱う）。

1. **運用に使う**秘密情報（APIキー・トークン・パスワード・署名鍵・本番の接続文字列等）の
   **実値を、エージェントが生成・表示・受領しない**。これはコード・コミット履歴・
   コミットメッセージ・issue/PR本文に加えて、**チャットの発言・ツールの出力・ログを含む
   すべての経路**に適用する。「デモ用だから」「仮の値だから」「後で差し替えるから」は
   例外にしない（その言い訳を認めた瞬間にこのルールは機能しなくなる）。
   - 実値が必要なときは、エージェントは**生成コマンドまたは生成手段だけを案内**し、
     利用者が自分の手元で生成して、保管先（Vercel の環境変数画面等）へ直接貼る。
     エージェントはその値を受け取らないし、確認のために言わせもしない。
   - 値が必要な変数は名前と用途だけを扱う。`.env.example` にはダミー値と用途だけ書き、
     実値は `.gitignore` で除外する運用を維持する。
   - 対象外1：**自動検査がプロセス内で作って捨てる値**（例: `scripts/smoke.sh` が
     検査用のセッションを組み立てるためだけに使う鍵）。
     この種の値は、①検査プロセスの中だけで使う ②表示・出力・保存しない
     ③どこにも登録せず終了時に消える、の3つを満たす限り生成してよい
     （満たさなくなった時点で運用の秘密情報として上のルールに戻る）。
   - 対象外2：**公開が前提の値**（Supabase の Publishable key 等、クライアントに
     配布される設計のもの）。ただし「公開前提かどうか」は提供元の公式ドキュメントで
     確かめてから判断する。迷ったら秘密情報として扱う。
   - **誤って実値が経路に出てしまったら、弁解も誇張もせず、事実だけで報告する。**
     このとき「露出した事実」と「運用への影響」を混ぜない。**経路に出た時点で
     露出は起きている**（未使用でもこれは変わらない）。そのうえで影響を別に判定し、
     その値が既に運用に入っていれば漏洩として扱って、利用者にエージェントを
     経由しない再生成と差し替えを依頼する。どこにも登録していなければ
     認証への影響は無く、破棄すれば足りる。どちらの場合も `docs/failures.md` に
     記録する。「たぶん大丈夫」で済ませないのと同様に、反省を示すために被害を
     大きく言うこともしない（どちらも事実の歪曲）。
2. 実在する第三者を特定できる情報（本名・個人の電話番号・住所・実メールアドレス等）を
   サンプルデータ・テストデータ・ドキュメントに書かない。ダミーデータを使う。
3. コミットメッセージ・コードコメント・PR本文・ドキュメントに、愚痴・内輪ネタ・攻撃的な
   言葉を書かない。前後の文脈を知らない第三者が読んでも意味が通る言葉で書く。
4. 動かない・使われていないコード（コメントアウトされた死んだコード、デバッグ用の
   `console.log` 等）を残したままコミットしない。
5. 他者の著作物（画像・コード・文章）を権利関係を確認せずに含めない。出所が分からない
   素材は使わない。
6. 「完了の証明」節のとおり、チェックを緑にするためにテスト・lintを削除・無効化・緩和しない
   （見せかけの品質を作らない）。

## 業務ドメインを決めるときの作法

この製品が扱う領域が、**書式も手順も法律も、既に外の世界で決まっている実務**（許認可・
業界標準の様式・会計や税制の手続き等）に触れる案件では、この節に従う。触れない案件
（社内ツール・純粋なUI/ロジックの製品等）では対象外。
（この節が生まれた経緯は `docs/failures.md` の該当項目を見る。実物を見ずに設計を決め、
利用者に確認するたび全部外れた案件があった。）

**製品の定義そのもの（誰が使い、何を差別化点とし、どの書類・成果物を出すか）は
`docs/design.md` に置く。実装の前にまずこれを読む。** `docs/handoff.md` は毎回上書きされる
揮発的なメモで、恒久的な決定を置く場所ではない。

1. **リポジトリ内の記録を先に読む。** Webを探す前に `README.md` / `docs/` / 既存コードを
   読む。一次情報のリンクが既に書いてあることがある。
2. **一次情報で決める。** 業界様式・法令要件は、発行元の原本（官公庁・業界団体のPDF等）に
   当たって決める。解説記事の孫引きで断定しない。**リンクの無い「業界ではこうなっている」を
   設計文書に書かない。**
3. **「標準」と言う前に、経路の異なる実物を複数そろえて一致を確認する。** 1件見て標準と
   決めない。同じ系列のサイトばかり見ない。**一致した項目だけを標準として採用し、
   ばらついた項目は「こちらの裁量」と明記して分ける。**
4. **原本に印字されている事項と、記入者の運用を混同しない。** 記入例にそう書いてあることは、
   様式がそう定めていることを意味しない。
5. **差別化点を主張する前に競合を調べる。** 「これが強みになる」と言う前に、既存製品が
   そこを主戦場にしていないかを確認する。
6. **成文化されていない前提は調査では取れない。** 実運用の前に、**実務者1人による現物
   レビューを必ず挟む**。調査で取れるのは「文章になっている層」だけで、現場の常識は
   本人が言語化しないため出てこない。
7. **利用者の判断は、その場で恒久的な記録に落とす。** 揮発的なメモに書いて次のセッションで
   消え、同じ指示を2回させないようにする。

## コマンド

- 開発サーバ: `pnpm --filter web dev`（初回のみ `pnpm install`）
- テスト（全件 / 1件だけ）: `pnpm --filter web test` / `pnpm --filter web test -- tests/content.test.ts`
- lint / 型チェック: `pnpm --filter web lint` / `pnpm --filter web typecheck`
- 起動スモーク（受け入れ条件を HTTP で機械判定）: `bash scripts/smoke.sh`（先に `pnpm -r build`）
- E2E（実ブラウザで一連の操作を機械判定）: `bash scripts/e2e.sh`（先に `pnpm -r build` と、
  初回のみ `pnpm --filter web exec playwright install --with-deps chromium`）
  （アプリの起動・停止は Playwright（`apps/web/playwright.config.ts`）に任せる）
- データの保存先: 未記入（案件で決まったら、接続方法・ローカル/CIでの起動方法・
  マイグレーションの適用方法をここに書く）
- 必須の環境変数（名前だけ。値は書かない）: 未記入（`apps/web/.env.example` 参照）
- 共通の文言・値の置き場所: `apps/web/lib/content.ts`
- 金額計算の置き場所: 未記入（案件で金額計算が要るなら、画面・PDF・API が必ず通す
  1箇所をここに書く）

補足（CI が回すもの）: ビルドは `pnpm -r build`、依存監査は `pnpm audit --audit-level moderate`。

注意（値が結合している箇所）: トップページの見出し `HOME_HEADING` は、CI の起動スモークが
本文から探すマーカーでもある。`apps/web/lib/content.ts` と `scripts/smoke.sh` の `MARKER` の
2箇所を同時に直す。公開URLができて `presets/prod-smoke.yml` を `.github/workflows/` に
コピーしたら、そこの `MARKER` も3箇所目として同時に直す。

## 実装の進め方

- 機能ごとに、中身を書く前に入口から出口までを1回通す。途中の各段は空の返事でよい。通ってから中身を実装する。
- **PR はフェーズにつき1つ**。フェーズの途中は同じブランチに draft のまま積み続け、
  そのフェーズの機能が全部そろってローカル検証（typecheck / lint / test / build /
  `scripts/smoke.sh`）が通ってから draft を解除する。レビューと自動マージはそこで1回だけ走る。
  途中でレビューだけ欲しいときは、マージせずに `@coderabbitai review` を呼ぶ。
  （小さいPRを連発すると、squash マージのたびにブランチを作り直す必要が生じ、
  レビューのレート制限にも当たる。フェーズ1で実際に両方起きた）
- **squash マージ後に同じブランチへ積み足さない。** マージ完了の通知を受けたら、
  次の変更に着手する前に必ず `git fetch origin main && git checkout -B <branch> origin/main`
  でブランチを作り直す（`docs/failures.md` 2026-08-03 / 2026-08-04 に同じ失敗が2回ある）。
- **利用者に画面を確認してもらうときは、本番に反映してから頼む。**
  Vercel のプレビューURLを渡さない。プレビューはデプロイ保護が掛かっていて
  スマホから開けず、確認してもらうために利用者へ設定変更を頼むことになる。
  **利用者に設定変更を頼まない**（2026-08-06 に実際にこれをやって時間を溶かした。
  利用者からの明示指示）。確認が要るものは main にマージして本番URLで見てもらう。
- 規約: なし

## 結合を増やさない

判定は「これを変えたとき何ファイル触るか。そのとき、他のファイルの中身まで読む必要があるか」。1ファイルで済み、他は呼び出し方だけ知っていれば足りる状態を保つ。

1. 同じ値・同じ文言を2箇所以上に書かない。上の置き場所に1つ書いて参照する。
2. 同じ処理を呼ぶ入口は1つにする。
3. 別々の機能が、同じ変数・同じ画面部品を書き換えない。
4. 他の機能の内部を直接触らない。公開された呼び出し方だけを通す。

行数上限の lint が鳴ったら、行数で割らず、後から単独で直したくなる塊ごとに分ける。全体を組み立てるファイルには実装を書かず、部品を呼ぶだけにする。

## 完了の証明

- 証拠は外部事実のみ: CI の run URL / commit SHA / 公開 URL。自己申告は証拠にしない。
- 緑にするためにチェックを削除・無効化・緩和しない。落ちたら実装側を直す。
- 中身のないテストを置かない。
- 失敗したことは `docs/failures.md` に追記する。**既存行は消さない。訂正も追記でやる**
  （過去の記述が誤っていたと分かっても、その行は残したまま、新しい日付の項目として
  「どの項目の何が誤りで、正しくはどうか」を書く）。このログの価値は、そのとき何を
  正しいと思っていたかが残ることにある。上書きしてよいのは `docs/handoff.md` だけ。

記録の使い分け（3つある。混ぜない）:

| ファイル | 中身 | 書き換え方 |
|---|---|---|
| `docs/design.md` | 製品の定義（誰が使うか・差別化点・出す書類・調査で確定した事実） | 追記と改訂。決定を変えるときは、変えた理由も残す |
| `docs/failures.md` | 失敗の履歴 | append-only。既存行を消さない。訂正も追記でやる |
| `docs/handoff.md` | セッションをまたぐ揮発的な状態 | 毎回上書き。**恒久的な決定をここに置かない** |
